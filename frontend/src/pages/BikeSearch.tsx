import {Container, Typography} from "@mui/material";
import Layout from "./Layout.tsx";

function BikeSearch() {

    return (
        <Layout>
        <Container>
            <Typography>Let's look for a bike</Typography>
        </Container>
        </Layout>
    )
}

export default BikeSearch